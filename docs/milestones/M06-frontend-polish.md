# Milestone 6 — Frontend polish (theme + auth-refresh fix + debug revamp)

> The site has accumulated style drift, the debug page needs both more data and better organization, and a long-standing auth-hydration race redirects admin pages to home on hard refresh. Cluster these three so the visual + dev-tooling foundation is solid before bigger feature work (M9 kiosk, M10 overlays).

## 6a. Auth-refresh redirect fix (frontend + backend audit)

Hard-refreshing `/containers/` or `/admin/debug/` bounces to home.

*Frontend root cause (primary).* In [sveltekit/src/lib/stores/auth.svelte.ts](../sveltekit/src/lib/stores/auth.svelte.ts), `auth.ready` is captured at module init, but on a cold CSR hydration the `+page.ts` load function calls `requireAdmin()` ([sveltekit/src/lib/utils/guards.ts](../sveltekit/src/lib/utils/guards.ts)) before the auth store has hydrated `isAdmin` from `/api/me`. Both [routes/admin/debug/+page.ts](../sveltekit/src/routes/admin/debug/+page.ts) and [routes/admin/debug/[name]/+page.ts](../sveltekit/src/routes/admin/debug/[name]/+page.ts) `await auth.ready` already, but the promise resolved instantly with stale state. Options: (a) move the `/api/me` fetch into a root `+layout.ts` load that all child routes inherit, (b) make `auth.ready` rebuild on every `pb.authStore` change rather than capture once. Decide during implementation.

*Backend gating audit.* Sweep [internal/pocketbase/routes/](../internal/pocketbase/routes/) for parallel inconsistencies — guards that should fire but don't, or fire too eagerly. Focus on:
- [internal/pocketbase/routes/middleware/auth.go](../internal/pocketbase/routes/middleware/auth.go) and [admin.go](../internal/pocketbase/routes/middleware/admin.go) — confirm the `RequireAuth` + `RequireAdmin` chain matches what's documented in CLAUDE.md (PB superusers + `users.isAdmin=true`).
- [internal/pocketbase/routes/admin/](../internal/pocketbase/routes/admin/) group registration — every admin route should inherit the chain, not re-add or skip.
- [internal/pocketbase/routes/me.go](../internal/pocketbase/routes/me.go) — currently exposes `{isAdmin, isSuperuser}` to the caller; verify it doesn't leak other users' admin status.
- [internal/pocketbase/routes/containers/](../internal/pocketbase/routes/containers/), [scraper/](../internal/pocketbase/routes/scraper/), [xemu/](../internal/pocketbase/routes/xemu/) — confirm each registered handler is gated.
- [internal/pocketbase/routes/allroutes.go](../internal/pocketbase/routes/allroutes.go) and [allgroups.go](../internal/pocketbase/routes/allgroups.go) — registration order.

If the frontend fix exposes a backend route that should have been guarded but wasn't, fix both at once.

Smoke test: hard-refresh `/containers/` and `/admin/debug/<name>/` while logged in as admin → page loads, no home bounce. Hit each `/api/admin/*` endpoint as anon, as a non-admin user, and as an admin → 401/403/200 respectively.

## 6b. Custom Skeleton theme + style consistency

Today the cerberus theme is loaded statically via [sveltekit/src/routes/layout.css](../sveltekit/src/routes/layout.css) (`@import '@skeletonlabs/skeleton/themes/cerberus'`) and the root sets `data-theme="cerberus"` in [sveltekit/src/app.html](../sveltekit/src/app.html). Define a project-branded theme — likely via Skeleton v4's theme generator or a hand-written `tailwind.config.ts` with custom design tokens. Audit pages for inconsistent spacing, button variants, and card patterns; centralize repeating chrome into reusable components. Pages to audit: `/`, `/login/`, `/containers/`, `/containers/[name]/`, `/admin/debug/`, `/admin/debug/[name]/`, `/overlays/players/`.

Smoke test: visual diff before/after; dark + light mode both render cleanly; OBS overlay backdrop stays transparent (overlays must remain unaffected by theme background).

## 6c. Debug page revamp + scraped-data validation

Existing tabs (Overview / Game / Tick / Events / Probe / Raw JSON) in [sveltekit/src/routes/admin/debug/[name]/+page.svelte](../sveltekit/src/routes/admin/debug/[name]/+page.svelte) and components in [sveltekit/src/lib/components/debug/](../sveltekit/src/lib/components/debug/). Scope:

- **Data coverage audit.** Walk every field surfaced by the Halo: CE reader ([internal/scraper/haloce/reader.go](../internal/scraper/haloce/reader.go), `offsets.go`, `offsets_reference.go`); confirm each renders somewhere in the debug UI. Promote currently-buried fields where useful.
- **Restyle.** Apply M6b theme; replace `KvCard` / raw JSON dumps with structured tables for high-volume fields (objects, projectiles).
- **Verification harness.** For fields tagged `unverified` in the offset table, surface a per-field "looks plausible / clearly broken / unknown" annotation as a manual-validation pass, feeding M19's runtime offset validation.
- **Probe tab.** Audit the existing probe outputs (`BuildScoreProbe`, `LastStateInputs`) and add probes for any field cluster currently untrusted.

Smoke test: 4-instance system-link match (same harness as M5 5c+5d+5e smoke), walk every tab on every instance, log any field that displays empty/zero/garbage and create offset-investigation follow-ups for M19.

## Log

_Append-only. Never edit past entries; add a new dated line._

- 2026-05-17: M6c — Overview tab gains a Handshake section showing the v2 hello envelope (protocol/classes/instances). Placeholder tabs added for `container`, `debug`, `objects`, `scenario`, and `summary` so the debug page's tab strip matches the planned envelope-class taxonomy.
- 2026-05-17: M6c — Xbox tab redesigned as a pure view of the `xbox` envelope. Pretty/JSON toggle (Skeleton `Switch` with `LayoutGrid`/`Braces` icons) lives in a new envelope-stats header (`seq`/`tick`/`received`/`v`/`instance`). Pretty view sections mirror the JSON shape (`top-level` / `time_zone` / `xbe` / `kernel`) with every field always rendered — missing values show `—`. JSON view pairs a Skeleton `TreeView` with a scoped `CodeBlock` (selecting a tree node returns the envelope wrapped down to that path). The `viewMode` is page-level state persisted to `localStorage['debug.view']` so the choice carries across future tabs that opt into the same toggle. WS v2 store now exposes `xboxEnvelope[instance]` alongside the existing payload-only `xbox[instance]`. The leftover `SystemTab` + `runtime-vm` mixed transport/runtime sections were dropped.
- 2026-05-17: M6c — Scenario tab migrated to the xbox Pretty/JSON pattern. New `debug/scenario/` folder mirrors `debug/xbox/`: `ScenarioTab` orchestrator, `ScenarioStatsHeader` (envelope-stats + Pretty/JSON Switch), `ScenarioPretty` (Accordion sections `top-level` / `fog` / `memory_regions` / `object_types` / `player_spawns` / `power_item_spawns` / `tag_defs` with collapse state persisted to `localStorage['debug.scenario.collapsed']`), `ScenarioJson` (TreeView + scoped CodeBlock), `scenario-vm.ts` pure formatters, and a duplicated `json-scope.ts` typed against `ScenarioPayload`. List sections render via the canonical `DataTable` with `defaultSort` + `secondarySort` for stable order. WS v2 store now exposes `scenarioEnvelope[instance]` alongside the payload-only `scenario[instance]`. New Playwright spec at `e2e/screenshots/debug-scenario.spec.ts` covers Pretty/JSON toggle on a mocked live instance.
- 2026-05-17: M6c — Game tab full rewrite on the xbox Pretty/JSON pattern. New folder layout (`GameTab` orchestrator + `GameStatsHeader` + `GamePretty` + `GameJson` + pure `game-vm` formatters + duplicate `json-scope` keyed on `GamePayload`). Pretty view is a Skeleton `Accordion` of `top-level` / `config` / `team_scores` / `players` / `machines` / `network` sections — nested-scalar groups render as `StatTile` grids, array sections (`team_scores`, `players`, `machines`) render via the canonical `DataTable` with one column per JSON field. JSON view mirrors XboxJson (TreeView + scoped CodeBlock). WS v2 store gains `gameEnvelope[instance]`. The pre-existing `v2ToV1GameData` adapter (still consumed by overview + postgame view-models) moved to `debug/shared/v2-to-v1-game.ts`. Old per-section components removed (FogSection, GameInfoSection, MachinesSection, ObjectTypesSection, PlayersSection, PowerItemsSection, SpawnsSection, TagCacheSection). Playwright spec `debug-game.spec.ts` covers tab activation, envelope-stats header, all six Pretty section headings, JSON-view toggle, and TreeView render.
- 2026-05-17: M6c — Tick tab rewritten on the same xbox Pretty/JSON pattern. Envelope-stats header carries the Pretty/JSON Switch. Pretty view is an Accordion of five sections that mirror the v2 `TickPayload` JSON shape (`players` / `power_items` / `ctf_flags` / `game_globals` / `locals`); every section renders even when empty so the operator can distinguish "no data yet" from "field not on the wire". JSON view follows the xbox pattern (`TreeView` + scoped `CodeBlock`). The v2→v1 tick projection that was buried in `tick-vm.ts` for the v1-typed Overview/Postgame sections moved into `overview-vm.ts` (the only remaining caller); the new `tick-vm.ts` is a pure formatter that returns `TickPrettyVm`. WS v2 store gains `tickEnvelope[instance]`. Playwright spec `debug-tick.spec.ts` asserts the envelope header, Pretty section headings, and the Pretty/JSON toggle.
- 2026-05-17: M6c — Objects tab migrated from placeholder to the xbox Pretty/JSON pattern. Pretty view renders an Accordion with `summary` (count tiles for `objects`/`projectiles`), `objects` (DataTable: id/tag/type/flags/pos.x-y-z/ang_vel.x-y-z/time_existing/owner_unit/owner_object/ultimate_parent — 0xFFFFFFFF owner sentinels collapse to `—`), and `projectiles` (DataTable: id/tag/pos.x-y-z/flags/action/detonation_timer/distance_traveled). JSON view reuses the Skeleton `TreeView` + scoped `CodeBlock` pattern. Accordion collapse state persists to `localStorage['debug.objects.collapsed']`. WS v2 store now exposes `objectsEnvelope[instance]` alongside the existing payload-only `objects[instance]`.
- 2026-05-17: M6c — Debug envelope tab migrated from placeholder to the xbox Pretty/JSON pattern. Envelope-stats header surfaces `seq`/`tick`/`received`/`v`/`instance` and the page-level Pretty/JSON toggle. Pretty view renders three Accordion sections (`players`, `state_inputs`, `score_probe`) mirroring the JSON shape: each `DebugPlayer` row is broken into `extended` / `bones` (table) / `update_queue` / `raw` (hex-keyed tiles) with a `present`/`null`/`missing` status badge so an operator can tell wire-side null apart from "not yet observed"; `state_inputs` and `score_probe` are rendered as alphabetized key/value tiles. JSON view follows the xbox pattern (Skeleton `TreeView` + scoped `CodeBlock`). WS v2 store gains a `debugEnvelope[instance]` slot. Accordion collapse state persists to `localStorage['debug.debug.collapsed']`.
- 2026-05-17: M6c — Previous-Game tab rewritten on the same xbox Pretty/JSON pattern. Envelope-stats header (`seq`/`tick`/`received`/`v`/`instance`) plus Accordion sections mirroring the JSON shape (`top-level` / `game` / `events`). The `game` section surfaces a summary of the nested `GamePayload` (phase, started_at, gametype/variant, score_limit, team_scores summary, players/machines counts) and defers the full live view to the dedicated Game tab. The `events` section renders the per-game envelope list as a sortable `DataTable` over `seq`/`tick`/`ts`/`type`/`data.event_type`. JSON view pairs `TreeView` + scoped `CodeBlock`. WS v2 store now exposes `previousGameEnvelope[instance]` alongside the existing payload-only `previousGame[instance]`. The old `postgame/` folder (`PostgameTab` + WinnerHeadline + FinalScoreboardSection + PlayerTotalsSection + EventLogSection + EmptyState) is gone — renamed to `previous_game/` to match the envelope class, and the helpers other tabs still need (`PlayerTotalRow` + `buildPlayerTotals`, `v2ToV1PreviousGame`) were lifted into `debug/shared/`. Collapsed-section state for the new tab persists to `localStorage['debug.previous_game.collapsed']`.
- 2026-05-17: M6c — Events tab gains the same envelope-stats header + Pretty/JSON `Switch` pattern. Header tiles surface the latest event envelope's `seq` / `tick` / `received` / `event_type` and the rolling-log size (`logged`), falling back to `—` when no events have arrived. The existing rolling feed (TypeFilter + EventFeed) is preserved verbatim under Pretty. JSON view pairs a Skeleton `TreeView` with the rolling log as a list of `seq:tick event_type` nodes — selecting one scopes the CodeBlock to that envelope (or a sub-path within it via the per-event `json-scope.ts`).
- 2026-05-18: M6c — post-rollout envelope-data audit on live `debug-host` / `debug-alpha` (Halo: CE, active multiplayer). Three issues surfaced by the new headers/tabs are now queued for fix:
  1. `seq` hardcoded to `0` in `internal/scraper/manager/runner.go:445`'s `MakeEnvelope` call — no per-(instance, class) counter exists. Every envelope class on every instance reports `seq: 0` over the wire. Fix: add `seqByClass map[string]uint64` + mutex to the runner struct, bump per `marshalClassEnvelope` call. P0.
  2. `xbox.name` empty on both live instances. Confirmed via `curl /api/admin/scraper`. The reader (`internal/scraper/xbox/console_name.go`) scans only `0x80000000–0x90000000` for the dashboard's `NICKNAME.XBN` magic anchor; containers that boot straight into Halo never load the dashboard, so the record is never in that window. Fix: add `xbox/console_name_eeprom.go` that reads the console name from EEPROM (mirrors the existing `serial.go` / `mac.go` shape — title-agnostic, always present once the kernel boots), and try EEPROM first in `loop.go:454` with the GVA scan as a fallback. P1.
  3. `scenario.map` empty during an active multiplayer match. The address `AddrMultiplayerMapName = 0x2E37CD` in `internal/scraper/haloce/offsets.go:93` is a `halocaster.py:1892`-port and remains unverified. Re-derive against a live `debug-host` / `debug-alpha` memory dump; if the offset is right but only transiently populated, add a fallback that reads the scenario tag's name string (constant offset off `AddrGlobalScenarioPtr`). M19 candidate but small enough to land alongside the other two. P1.
- 2026-05-18: M6c — Fix A (seq counter) landed. `internal/scraper/manager/runner.go` — `runner` gains `seqByClass map[string]uint64` + `seqMu`, exposed via `nextSeq(class)`; `marshalClassEnvelope` calls it before `MakeEnvelope`. Each envelope class now ships a monotonic per-(instance, class) counter; clients can detect drops / out-of-order delivery.
- 2026-05-18: M6c — Fix B (xbox.name) landed. Triangulated against all four live containers (`debug-host` Whicker, `debug-alpha` Cupid, `debug-bravo` Crazy, `debug-charlie` charr) and found a common 12-byte anchor that works regardless of which XBE is loaded: `4B 43 49 4E 01 00 00 00 00 00 00 00` ("NICK" + u32(1) + 4 zero bytes). The old 8-byte anchor in `xbox/console_name.go` (`00 00 00 00 30 53 11 9E`) only matched record type B (canonical `NICKNAME.XBN` with `ConsoleNameRecordMagic` 0x9E115330), which is the format the dashboard loads; containers running Halo (or anything other than a fully-booted dashboard) only have record type A (`04 00 53 4D` tag, no length field, name follows immediately at NICK+16), which the old scanner missed. New scanner walks the same 0x80000000–0x90000000 window for the NICK anchor, dispatches on the 4-byte record tag at NICK+12: type A → name UTF16LE at NICK+16; type B → 2-byte length (`20 00` = 32 bytes max) then name at NICK+18. Verified live on all four containers returning their correct nicknames.
