# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/

# Generated
src/lib/types/pocketbase-types.ts

# Test output
/test-results/
/playwright-report/

# Vendored wire artifacts (byte-identical to ../xc-scraper/wire/*; task sync-wire).
# Never formatted here: sync-wire:check / CI diff them byte-for-byte against the
# sibling, and prettier would break that.
src/lib/types/scraper-v2.ts
src/lib/types/wire-fixtures/
